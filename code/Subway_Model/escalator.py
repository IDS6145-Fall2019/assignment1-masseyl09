class escalator:
    ''' A escalator class '''

    def __init__(self, length =60):
        self.length = length
        print("initialization of one escalator that is "+ str(self.length)+" feet in length")